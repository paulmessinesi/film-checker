services:
  film-checker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000"
    environment:
      - DB_PATH=/data/films.db
    volumes:
      - film-checker-data:/data
    restart: unless-stopped

volumes:
  film-checker-data:
